{
  "name": "gurukul-home-tuitions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "lint": "node --check server.js && node --check api/data.js && node --check api/upload.js && node --check api/delete-blob.js && node --check lib/db.js",
    "build": "echo 'No build step required'"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "@vercel/blob": "^0.27.0",
    "cloudinary": "^2.10.0",
    "express": "^5.2.1"
  }
}
